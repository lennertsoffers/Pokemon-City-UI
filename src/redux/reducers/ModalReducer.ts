import ActionTypeEnum from "../../types/enums/ActionTypeEnum";

const ModalReducer = (state: any, action: { type: ActionTypeEnum; modal: String }) => {
    switch (action.type) {
    }
};

export default ModalReducer;
